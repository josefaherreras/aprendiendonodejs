extends ./layout.jade
//identificador
block contenido
    form(action="/users", method="POST")
        div(class="form-group")
         label(for="email") Email
         input(type="email", name="email" placeholder="ingrese su Email" id="email" class="form-control")
        div
         label(for="password") Contraseña
         input(type="password", name="password" placeholder="ingrese su Contraseña" id="password" class="form-control")
        div
         input(type="button", value="Iniciar Sesión" class="btn btn-info btn-block")
