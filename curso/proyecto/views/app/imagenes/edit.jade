extends ../../layout.jade
//identificador
block contenido
    div(class="login col-md-6 remove-float center-block  big-top-space")
        h1 Subir nueva Imagen
        form(action="/app/imagenes/#{imagen.id}?_method=PUT", method="POST")
                div(class="form-group")
                    label(for="title") Títutlo
                    input(type="text", name="title" placeholder="Títutlo de la imagen" id="title" class="form-control", value="#{imagen.title}")
                    div(class="top-space")
                        input(type="submit", value="Guardar" class="btn btn-info btn-block")
    
            